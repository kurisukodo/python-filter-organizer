# python-filter-organizer

Python filter organizer is a simple python script that helps to organize any files in a folder based on file formats specified in a json file.

## Installation

Install python3 for your system and run the following command to execute the script

```base
python3 main.py \path-to-your-folder-to-organize
```
